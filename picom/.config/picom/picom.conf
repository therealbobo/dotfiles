
#refresh-rate = 0;
backend = "glx";
vsync = true;
use-damage = false;
unredir-if-possible = true;
dbe = false;
sw-opti = false;

shadow-exclude = [ 
	"name = 'noshadow'", 
	"name = 'polybar'", 
	"class_g ?= 'Notify-osd'" 
];

opacity-rule = [
	"90:class_g = 'termite'",
	"63:class_g = 'Bspwm' && class_i = 'presel_feedback'"
];
